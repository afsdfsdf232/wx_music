<view class="playlist-container" bind:tap="goDetail">
	<image src="{{playlist.picUrl}}" lazy-load="true" class="playlist-img"></image>
	<text class="playlist-playcount">{{count}}</text>
	<view class="playlist-name">{{playlist.name}}</view>
</view>